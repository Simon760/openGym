# Dependencies
node_modules/

# Build output
frontend/dist/
dist/

# Local environment
.env
.env.local

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# Runtime data — per-user state, passkeys, session secret and push keys.
# Both of these were committed by mistake when this fork was imported and have
# since been untracked. They still exist in the git history, so data/secret and
# data/vapid.json must be regenerated (delete them; the API writes fresh ones on
# next start) rather than treated as private.
data/
media/
